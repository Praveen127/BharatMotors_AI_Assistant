# Core agent framework (Track A: LangChain)
langchain>=0.2.0
langchain-openai>=0.1.0
langchain-community>=0.2.0
openai>=1.0.0

# Retrieval / vector store
faiss-cpu>=1.7.4
tiktoken>=0.6.0

# Knowledge ingestion
pypdf>=4.0.0

# Deployment
flask>=3.0.0

# Offline-mode fallback (used automatically when the above / network are
# unavailable -- see README "Offline Mode" and docs/engineering_justification.md)
scikit-learn>=1.3.0
numpy>=1.24.0

# Evaluation / utilities
python-dotenv>=1.0.0
